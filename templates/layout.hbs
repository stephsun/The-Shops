<!doctype html>
<html>
    <head>
        {{> cdn }}
        <title>The Shops - {{ title }}</title>
        {{{ block "javascripts" }}}
        {{{ block "stylesheets" }}}
    </head>
    <body>
        {{{ body }}}
    </body>
</html>