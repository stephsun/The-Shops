{{#extend "javascripts"}}
    <script type="text/javascript" src="/javascripts/main.js"></script>
{{/extend}}

{{#extend "stylesheets"}}
    <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
{{/extend}}

<div id="wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <ul class="nav nav-tabs">
                    <li role="presentation">
                        <a href="/"><span class="glyphicon glyphicon-ice-lolly"></span></a>
                    </li>
                </ul>

                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th data-field="order">Order</th>
                            <th data-field="name">Name</th>
                            <th data-field="url">Link</th>
                            <th data-field="actions">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                        <form action="/admin" method="post">
                            <td>
                            </td>
                            <td>
                                <input type="text" class="form-control" id="brandName" name ="longName" placeholder="e.g., J.CREW. The brand name you typed will appear exactly the same in the sidebar">
                            </td>
                            <td>
                                <input type="url" class="form-control" id="brandUrl" name="url" placeholder="e.g., https://www.jcrew.com/">
                            </td>
                            <td>
                                <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-plus"></span></button>
                            </td>
                        </tr>
                        </form>
                        {{#each brands}}
                        <tr>
                            <td>{{rank}}</td>
                            <td>{{longName}}</td>
                            <td>{{url}}</td>
                            <td>
                                <div class="btn-group" role="group" aria-label="...">
                                  <button type="button" class="btn btn-default"><span class="glyphicon glyphicon-pencil"></span></button>
                                  <button type="button" class="btn btn-default brand-delete"><span class="glyphicon glyphicon-trash"></span></button>
                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    <tbody>
                </table>

            </div>
        </div>
    </div>
</div>